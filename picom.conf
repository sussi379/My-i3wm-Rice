# Disable animations
fading = false;
animations = false;

# Enable transparency backend
backend = "xrender";
vsync = true;

# Kitty transparency (85% opaque = 15% transparent)
opacity-rule = [
  "85:class_g = 'kitty'"
];
